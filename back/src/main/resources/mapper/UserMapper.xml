<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.homemaking.mapper.UserMapper">

    <update id="updateStatus" parameterType="java.lang.Long">
        update sys_user
        set status = case when status = 1 then 0 else 1 end
        where id = #{id}
    </update>
    <update id="setUserStatus">
        update sys_user
        set status = case when status = 1 then 0 else 1 end
        where id = #{id}
    </update>
    <update id="updateUserPassword" parameterType="com.example.homemaking.entity.SysUser">
        update sys_user
        set pay_password = #{payPassword}
        where id = #{id}
    </update>
    <update id="updateUserPhone" parameterType="com.example.homemaking.entity.SysUser">
        update sys_user
        set phone = #{phone}
        where id = #{id}
    </update>
    <update id="saveAddress">
        insert into sys_user_address(user_account, target_id, label, consignee_name, consignee_phone, province, city,
                                     district, detail_address, is_default, create_time, update_time, is_deleted)
        values (#{userAccount}, #{targetId}, #{label}, #{consigneeName}, #{consigneePhone}, #{province}, #{city},
                #{district}, #{detailAddress}, #{isDefault}, #{createTime}, #{updateTime}, #{isDeleted})
    </update>
    <update id="updatePassword">
        update sys_user
        set password = #{newPassword}
        where phone = #{phone}
    </update>
    <update id="updateUserName" parameterType="com.example.homemaking.entity.SysUser">
        update sys_user
        set username = #{username}
        where id = #{id}
    </update>

    <select id="selectAll" resultType="com.example.homemaking.entity.SysUser">
        select *
        from sys_user
    </select>
    <select id="getUserById" resultType="com.example.homemaking.entity.SysUser">
        select *
        from sys_user
        where id = #{id}
    </select>
    <!-- 用户列表公共过滤条件（供 searchUsers / countUsers / searchUsersPage 复用） -->
    <sql id="userCondition">
        <if test="name != null and name != ''">
            and username like concat('%', #{name}, '%')
        </if>
        <if test="phone != null and phone != ''">
            and phone like concat('%', #{phone}, '%')
        </if>
        <if test="startTime != null">
            and create_time &gt;= #{startTime}
        </if>
        <if test="endTime != null">
            and create_time &lt;= #{endTime}
        </if>
    </sql>

    <select id="searchUsers" resultType="com.example.homemaking.entity.SysUser">
        select * from sys_user where 1=1
        <include refid="userCondition"/>
        order by id desc
    </select>

    <select id="countUsers" resultType="long">
        select count(*) from sys_user where 1=1
        <include refid="userCondition"/>
    </select>

    <select id="searchUsersPage" resultType="com.example.homemaking.entity.SysUser">
        select * from sys_user where 1=1
        <include refid="userCondition"/>
        order by id desc
        limit #{offset}, #{pageSize}
    </select>
    <select id="getUserByAccount" resultType="com.example.homemaking.entity.SysUser">
        select *
        from sys_user
        where account = #{account}
    </select>
    <select id="getAddressList" resultType="com.example.homemaking.entity.UserAddress">
        select *
        from sys_user_address
        where user_account = #{userAccount}
          and is_deleted = 0
    </select>
    <select id="getAccountPassword" resultType="java.lang.String">
    select pay_password
        from sys_user
        where account = #{account}
        and is_deleted = 0
    </select>
    <select id="selectCountByPhone" resultType="java.lang.Integer">
        select count(*) from sys_user where phone = #{phone}
    </select>
</mapper>